name: Siza Mana Burn Selection Migration

on:
  push:
    paths:
      - '.github/workflows/siza-burn-selection-migration.yml'
      - '.github/scripts/siza-burn-selection-migration.js'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate-burn-selection:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install --no-save jsdom@24
      - name: Validate old/new and publish runtime
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: node .github/scripts/siza-burn-selection-migration.js
