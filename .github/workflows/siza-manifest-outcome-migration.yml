name: Siza Manafestation Outcome Migration

on:
  push:
    paths:
      - '.github/workflows/siza-manifest-outcome-migration.yml'
      - '.github/scripts/siza-manifest-outcome-migration.js'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate-manifest-outcome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install --no-save jsdom@24
      - name: Validate old/new and publish runtime
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: node .github/scripts/siza-manifest-outcome-migration.js
