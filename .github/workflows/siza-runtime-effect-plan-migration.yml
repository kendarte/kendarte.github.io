name: Siza Runtime Effect Plan Migration

on:
  push:
    paths:
      - '.github/workflows/siza-runtime-effect-plan-migration.yml'
      - '.github/scripts/siza-runtime-effect-plan-migration.js'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate-runtime-effect-plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install --no-save jsdom@24
      - name: Validate old/new and publish runtime
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: node .github/scripts/siza-runtime-effect-plan-migration.js
