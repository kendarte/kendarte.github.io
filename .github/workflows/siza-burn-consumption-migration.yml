name: Siza Mana Burn Consumption Migration

on:
  push:
    paths:
      - '.github/workflows/siza-burn-consumption-migration.yml'
      - '.github/scripts/siza-burn-consumption-migration.js'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate-burn-consumption:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install --no-save jsdom@24
      - name: Validate old/new and publish runtime
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: node .github/scripts/siza-burn-consumption-migration.js
