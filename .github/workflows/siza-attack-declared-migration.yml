name: Siza Attack Declared Migration

on:
  push:
    paths:
      - '.github/workflows/siza-attack-declared-migration.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate-attack-declared:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install --no-save jsdom@24
      - name: Validate old/new and publish runtime
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: node .github/scripts/siza-attack-declared-migration.js
