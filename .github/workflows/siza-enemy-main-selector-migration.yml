name: Siza Rival Main Selector Migration

on:
  push:
    paths:
      - '.github/workflows/siza-enemy-main-selector-migration.yml'
      - '.github/scripts/siza-enemy-main-selector-migration.js'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate-rival-main-selector:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install --no-save jsdom@24
      - name: Validate old/new and publish runtime
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: node .github/scripts/siza-enemy-main-selector-migration.js
