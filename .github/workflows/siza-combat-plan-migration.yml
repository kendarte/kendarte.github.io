name: Siza Combat Plan Migration

on:
  push:
    paths:
      - '.github/workflows/siza-combat-plan-migration.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate-combat-plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install --no-save jsdom@24
      - name: Validate old/new and publish runtime
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPOSITORY: ${{ github.repository }}
        run: node .github/scripts/siza-combat-plan-migration.js
